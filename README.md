# codealpha
A Data Redundancy Removal System built using Python and Streamlit. It supports CSV upload, duplicate detection, data cleaning, SQLite database storage, and basic CRUD operations with validation.
